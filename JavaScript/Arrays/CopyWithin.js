// https://devdocs.io/javascript/global_objects/array/copywithin
// copies array elements to another position in an array, overwriting existing values:

// Syntax
// copyWithin(target)
// copyWithin(target, start)
// copyWithin(target, start, end)

const array1 = ['a', 'b', 'c', 'd', 'e','f','g']; // tem 7 indices

//console.log(array1.copyWithin(2, 0));
//console.log(array1.copyWithin(1, 2, -1));
