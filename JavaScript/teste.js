

// Sum Numbers
function sum(numbers) {
    let soma = 0;

    for (let i = 0; i < numbers.length; i++) {
        console.log(soma += numbers[i]);
    }
    console.log(soma) 
  }
  
  sum([1,3]);
  

 